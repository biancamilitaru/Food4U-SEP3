@page "/EditRestaurant"
@using Client.Data.RestaurantService
@using Entities
@inject Restaurant Restaurant
@inject IRestaurantService RestaurantService
<h3>Edit Restaurant</h3>


<EditForm OnValidSubmit="EditExisingRestaurant">
    <DataAnnotationsValidator/>
    <ValidationSummary/>
    <div class="form-group">Name of the restaurant:<br/>
        <InputText @bind-Value=""/></div>
    
    <div class="form-group">Address of the restaurant:<br/>
        <InputText @bind-Value=""/></div>
    
    <div class="form-group">Phone number of the restaurant:<br/>
        <InputText @bind-Value=""/></div>
    
    <div class="form-group">Opening hours: <br/>
        <p> Monday:<InputText @bind-Value=""></InputText>-<InputText @bind-Value=""></InputText><br/>
            Tuesday:<InputText @bind-Value=""></InputText>-<InputText @bind-Value=""></InputText><br/>
            Wednesday:<InputText @bind-Value=""></InputText>-<InputText @bind-Value=""></InputText><br/>
            Thursday:<InputText @bind-Value=""></InputText>-<InputText @bind-Value=""></InputText><br/>
            Friday:<InputText @bind-Value=""></InputText>-<InputText @bind-Value=""></InputText><br/>
            Saturday:<InputText @bind-Value=""></InputText>-<InputText @bind-Value=""></InputText><br/>
            Sunday:<InputText @bind-Value=""></InputText>-<InputText @bind-Value=""></InputText><br/>
        </p>
    </div>
    
    <div class="form-group">Description of restaurant:<br/>
        <InputText @bind-Value=""/></div>
    
    <div class="form-group">Delivery options:<br/>
        <input type="checkbox" id="delivery" name="Delivery"/>
        <input type="checkbox" id="pickup" name="Pickup"/>
    </div>
    
    <div class="form-group">Price of delivery:<br/>
        <p>
            Delivery:<InputText @bind-Value=""/><br/>
            Pickup:<InputText @bind-Value=""/>
        </p></div>
    <p class="actions"><button class="btn btn-outline-dark" type="submit">Save Changes</button></p>
</EditForm>




@code {

    public void EditExisingRestaurant()
    {
        
    }

    private Restaurant editExistingRestaurantItem()
    {
    }